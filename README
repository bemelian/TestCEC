Приложение реализует вывод видеопотока с web-камеры и в реальном времени находит и выделяет в bounding box лица,
расположенные фронтально к камере. На данный момент приложение умеет распознавать мое лицо (1 объект), лицо Криштиану
Роналду (2 объект) и неизвестные лица (-1). При закрытии приложение выдает номер объекта, который за время его работы
был распознан с наибольшей точностью.

Приложение не имеет интерфейса и тестировалось в PyCharm. Для корректной работы приложения на компьютере должны быть
установлены следующие компоненты:
numpy
opencv-contrib-python (opencv-python недостаточно)

Для безопасного закрытия приложения необходимо нажать 'q'.